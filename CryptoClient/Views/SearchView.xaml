<UserControl x:Class="CryptoClient.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CryptoClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.InputBindings>
        <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
    </UserControl.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding TextBoxContent, Mode=TwoWay}"
                 FontSize="49"
                 ToolTip="Input crypto currency name or code">
        </TextBox>
        <!--<TextBlock Text="Recent searching results:" FontSize="32" Grid.Column="1"/>-->

        <Button Grid.Column="1" Content="Serach" Command="{Binding SearchCommand}"/>
    </Grid>
</UserControl>
