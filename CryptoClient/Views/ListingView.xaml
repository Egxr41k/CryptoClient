<UserControl x:Class="CryptoClient.Views.ListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CryptoClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <ListView
                 ItemsSource="{Binding CryptoList}"
                 SelectedItem="{Binding SelectedListingItemViewModel}"
                 Height="400" Width="213">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        HorizontalAlignment="Left"
                        FontSize="12"
                        Text="{Binding CurrencyName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
